- **Quick-start: `CLAUDE.md` at repo root** — commands, conventions that bite (palette tokens, shared BackButton, file→dataURL helpers, the minimal surviving `ui/*` set), and layout pointers.
- [Crochet Time app structure](crochet-time-structure.md) — key files, character paths, ViewType navigation map, family-profiles system, tutorial/import systems, CSS conventions, the PR #48 cleanup pass (deleted ui components/deps, BackButton, palette tokens, useSectionEditing), and the #56–#60 program (server routes split into `server/routes/<domain>Routes.ts`; `routes.ts` is now a 70-line orchestrator).
- Delivery state lives in `docs/CROCHET_TIME_ROADMAP_2026-06-09.md` (SSOT): **roadmap fully delivered 2026-06-11** — Phases 1–8 + Phase 8 innovations all merged (PRs #23–#28); four test layers green in CI incl. real-Postgres full-stack smoke; **live-deploy verification PASSED 2026-06-20 — app fully launched, no gate items remain**; drizzle-migrations consciously deferred. **Post-2026-06-11 continuation** (see the roadmap's "Delivered since 2026-06-11" section): Calculators/Tools screen, durable work-time tracking + as-built `finishedRecord`, PDF import, and a large refactor/cleanup program (PRs #34–#45); unit tests 20→81; CI green on the latest `main` (#45). **Cleanup pass (PR #48, merged 2026-06-20):** 42 dead files + 33 unused npm deps removed (~4.4k LOC), file→dataURL helpers consolidated, shared `<BackButton>`, ~200 hex literals tokenised into `palette`, `usePatternViewer` split into `useSectionEditing` — details in the structure doc's "Cleanup pass" section. **Refactor/cleanup #56–#60 (merged 2026-06-22):** axios migration + `usePhotoUpload`/`EditableItemList`/`SegmentedControl`, colour tokens, and the full `server/routes.ts` split (1409→70-line orchestrator; per-domain modules under `server/routes/`).
- [PatternViewer tabs design](patternviewer-tabs.md) — viewer has Overview/Pattern/Notes tabs + a `pattern-viewer/*` component split with the `usePatternViewer`/`usePatternRegen`/`useSectionEditing` hooks; Notes persist in the DB (`patterns.notes`, debounced PUT — Phase 2 T1); Adapt panel is live.
- [PatternInput wizard](patternInput-wizard.md) — the 5-step Create wizard and the live `components/pattern-input/*` module split (constants/helpers/Pickers/AiWizard/OwnWizard/self-contained PdfWizard/WizardChrome).
- [Historical fixes & conventions](historical-fixes.md) — durable rules distilled from the pre-launch deep-review/audit/walkthrough passes: every mutation needs a destructive `onError`; every query needs an `isLoading` skeleton; send partial PUT bodies; invalidate shared caches; ARIA on icon/role-mismatched controls; plus architecture invariants (pattern lifecycle, active-project selection, `desc(createdAt)` order, dynamic community bell). Bug-by-bug detail is in git history.
- [Image & community system](image-community-system.md) — auto-image after POST /api/patterns (fire-and-forget); 40-pattern community seed; PatternThumb component; reseed threshold 30.
- [Profile seeding system](profile-seeding.md) — 4 profiles: larissa/vumsh/akka/mummy; seedProfilePatterns() in server/seedProfilePatterns.ts seeds 5 patterns each for non-Larissa profiles; flag: profile_content_seeded_v1; ensureSchema runs a one-time (`patterns_deduped_v1`-guarded) pattern dedup.
- [Household auth gate](household-auth-gate.md) — optional shared-passcode gate (`HOUSEHOLD_PASSCODE`, unset ⇒ off); server-enforced on all `/api/*` except `/api/auth/*` via a signed httpOnly `hh_session` cookie (`server/auth.ts`); client `AuthGate` wraps `<App/>`, a 401 re-locks via the `hh-auth-locked` event.
- [Push reminders](push-reminders.md) — opt-in web-push (VAPID keys ⇒ on), per-profile daily + inactive-project nudges; pure logic in `server/reminderLogic.ts` (unit-tested), orchestration in `reminders.ts`, `web-push` + `push_subscriptions` in `push.ts`; autoscale-safe via `POST /api/push/run-due` (CRON_SECRET); Settings `RemindersCard` + `sw.js` push handlers.
- [Imported source files](source-files.md) — PDF import now keeps the original: `patterns.sourceFiles` jsonb (object-storage keys), `POST/DELETE /api/patterns/:id/source-files`, `SourcePdfViewer` + a per-pattern **Source** tab & peek sheet, and a **Files** library (segment in `PatternLibrary`, `ViewType "documents"`/Sidebar). Pure helpers in `lib/documents.ts` (unit-tested).
- [PR merge conflict pattern](pr-merge-conflicts.md) — recurring conflict in SettingsScreen.tsx (icons + App health card) and routes.ts (seed imports + startup chain); resolution: combine all icons, keep both tour+health cards, chain seedStarterContentOnce→seedProfilePatterns→seedLibraryImages inside ensureSchema().then().
